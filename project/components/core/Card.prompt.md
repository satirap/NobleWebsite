White content surface — hairline ring, soft cool shadow, generous radius. The base container for services, stats, and panels.

```jsx
<Card hoverable accent="blue">
  <h3>Network Solution</h3>
  <p>LAN · Wi-Fi · Fiber Optic</p>
</Card>
```

Props: `hoverable` (lift on hover), `accent="blue"|"red"` (top keyline), `padding="none"|"sm"|"md"|"lg"`.
