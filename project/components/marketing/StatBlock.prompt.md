Large monospace metric with caption — for trust strips and proof points.

```jsx
<StatBlock value="99.9%" label="Uptime SLA" />
<StatBlock value="13+" label="ปีของประสบการณ์" tone="accent" align="center" />
```

Props: `value`, `label`, `align="left"|"center"`, `tone="brand"|"accent"|"light"` (use `light` on dark backgrounds).
